// @flow
import React from 'react';
import { StatusBar } from 'react-native';
import colors from '../colors';

function MyStatusBar() {
  return <StatusBar backgroundColor={ colors.green } />;
}

export default MyStatusBar;
