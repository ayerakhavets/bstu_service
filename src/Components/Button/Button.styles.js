// @flow
import { StyleSheet } from 'react-native';

const red = 'red';

export default StyleSheet.create({
  button: {
    alignItems: 'center',
    backgroundColor: red,
    justifyContent: 'center',
    width: '100%',
    height: 50,
    marginTop: 10
  }
});
